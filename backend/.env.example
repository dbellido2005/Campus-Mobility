# Email Configuration for Campus Mobility
# Copy this file to .env and fill in your actual values

# SendGrid Configuration
SENDGRID_API_KEY=your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=Campus Mobility

# Database Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=campus_mobility

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=720

# Uber API Configuration (Note: Rides API is deprecated, using fallback pricing)
UBER_CLIENT_ID=your-uber-client-id
UBER_CLIENT_SECRET=your-uber-client-secret
UBER_SERVER_TOKEN=your-uber-server-token
UBER_SANDBOX_MODE=true

# Google Maps API (for enhanced location and distance calculations)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Important Notes for SendGrid:
# 1. Sign up for a SendGrid account at https://sendgrid.com
# 2. Create an API key in the SendGrid dashboard
# 3. Verify your sender identity (single sender verification)
# 4. Use a verified email address as SENDGRID_FROM_EMAIL
# 5. Keep your API key secure and never commit it to version control

# Important Notes for Uber API:
# 1. Uber Rides API is deprecated for new applications as of 2025
# 2. Register at https://developer.uber.com for legacy access (if available)
# 3. We'll implement fallback pricing with enhanced calculation
# 4. Consider Uber Direct API for delivery-related features